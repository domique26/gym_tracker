import 'package:flutter/material.dart';

class CompareTrainigsPage extends StatelessWidget {
  const CompareTrainigsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        color: Colors.grey[800],
      ),
    );
  }
}
